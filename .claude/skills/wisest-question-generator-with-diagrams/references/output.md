# Diagrams Generator — Output Template + Escaping (STUB — RESTORE REQUIRED)

> ⚠️ Not recovered from the sandbox. Should hold the exact file template and the
> escaping rules for embedding Mafs JSX inside the TypeScript `Question[]` literal.
> For the non-diagram fields, the canonical contract is `references/schema.md`
> (copied into this folder). Restore your original for the JSX-in-.ts specifics.
