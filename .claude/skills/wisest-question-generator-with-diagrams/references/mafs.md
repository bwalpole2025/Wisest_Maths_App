# Diagrams Generator — Mafs API + Schema (STUB — RESTORE REQUIRED)

> ⚠️ This file could NOT be recovered from the sandbox. It is the Mafs component API
> and the `Step.diagram` schema your diagrams generator depends on, and it cannot be
> faithfully reconstructed without your originals. The diagrams generator will not
> produce correct Mafs JSX until you restore this file from your repo / git history.
>
> It should document at minimum:
>   - which Mafs components and version you target (Coordinates, Plot, Vector, Point,
>     Polygon, Line, Circle, etc.) and their import path;
>   - the exact shape of the `Step.diagram` field in your Question schema (the JSX/string
>     contract the frontend renders);
>   - your colour tokens and any wrapper/escaping rules for embedding JSX in the .ts file.
>
> Until restored, route `visual: true` manifest topics through
> `wisest-undergrad-question-generator` (text-only) — see the bundle INSTALL.md.
